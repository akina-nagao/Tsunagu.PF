  <% post_members.each do |post_member| %>
    <% post = post_member.post %>
    活動名：<%= post.title %><br>
    投稿者：<%= post.customer.nickname %><br>
    参加者：<%= post_member.customer.nickname %>｜
    id：<%= post_member.id %>
    <hr>
  <% end %>
  <%= link_to "戻る", mypage_path, class: "btn btn-secondary" %>