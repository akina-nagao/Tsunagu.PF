<%= content_for(:bg_area) do %>
  <div class="jumbotron jumbotron-fluid">
    <div class="container h-100 d-flex">
      <div class="my-auto">
        <h1 class="d-flex justify-content-center">今がつながるとき</h1>
        <div class="mt-4 d-flex justify-content-center">
          <%= link_to "参加する", new_customer_registration_path, class: "btn btn-primary" %>
        </div>
        <p class="mt-4 d-flex justify-content-center">Bootstrap is the most popular HTML, CSS, and JS framework for developing responsive, mobile-first projects on the web.</p>
      </div>
    </div>
  </div>
<% end %>
